## Why

## What changed

## How I tested

- [ ] `npm test`
- [ ] `npm run typecheck`
- [ ] `node dist/cli.js health` (after build)

## Notes

Keep the CLI read-only unless this PR is explicitly adding a gated write path.
